{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "functions": {
    "app/api/analyze/route.ts": {
      "maxDuration": 180
    }
  },
  "crons": [
    {
      "path": "/api/cron/digest?key=${CRON_SECRET}",
      "schedule": "0 9 * * *"
    }
  ]
}
